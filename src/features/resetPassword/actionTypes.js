// @flow
import { NAME } from './constants';

export const RESET_PASSWORD = `${NAME}/RESET_PASSWORD`;
export const RESET_PASSWORD_SUCCEEDED = `${NAME}/RESET_PASSWORD_SUCCEEDED`;
export const RESET_PASSWORD_FAILED = `${NAME}/RESET_PASSWORD_FAILED`;

export const REFRESH = `${NAME}/REFRESH`;
